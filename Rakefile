begin
  require "jeweler"
    Jeweler::Tasks.new do |gem|
        gem.name = "spud_events"
        gem.summary = "Spud Events Engine"
        gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]
        gem.authors = 'Dustin DeYoung'
        gem.email = 'dustin@westlakedesign.com'
	end
rescue
	puts "Jeweler or one of its dependencies is not installed."
end
