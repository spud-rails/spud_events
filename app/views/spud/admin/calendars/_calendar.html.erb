<li id="spud_events_admin_calendar_legend_<%= calendar.id %>">
  <%= content_tag :span, calendar.title, :style => "background-color: \##{calendar.color}"%>
  <div class="spud_events_admin_calendar_actions">
    <%= link_to 'edit', edit_spud_admin_calendar_path(calendar) %>
    <%= link_to 'delete', [:spud_admin, calendar], :remote => true, :confirm => 'Are you sure?', :method => :delete %>
  </div>
</li>
